    <nav class="navbar navbar-expand-lg navbar-light bg-primary">
      <a class="navbar-brand" href="#">DorC</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
          <li class="nav-item active">
            <%= link_to "Home", root_url ,:class => "nav-link" %><span class="sr-only">(current)</span>
          </li>
          
            <% if session[:user_id]==nil %>
              <li class="nav-item">
                <%= link_to "Login" ,login_url ,:class => "nav-link" %>
              </li>
            <% else %>
              <li class="nav-item dropdown">
             <a class="nav-link dropdown-toggle"  id="dropdown10" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= session[:username] %></a>
              <div class="dropdown-menu" aria-labelledby="dropdown07">
                <%= link_to "Show" ,user_url(session[:user_id]) ,:class=>"dropdown-item" %>
                <%= link_to "Edit" ,edit_user_url(session[:user_id]) ,:class=>"dropdown-item" %>
                <%= link_to "Logout" ,logout_path,:class=>"dropdown-item",method: :delete %>
              </div>
            </li>
            <% end %>
          

          <li class="nav-item">
            <a class="nav-link disabled" href="#">Disabled</a>
          </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
          <input class="form-control mr-sm-2" type="search" placeholder="Search">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>
      </div>
    </nav>